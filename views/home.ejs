<%- include('partials/header') -%>
<h1>Home</h1>
<p><%= homeContent %></p>

<% posts.forEach(function(post) { %>
    <h1><%= post.Title %></h1>
    <p><%= post.Body.slice(0,100) + " ..." %>
        <a href="/Titles/<%=post.Title%>">Read More</a>
    </p>
  <% }); %>

<%- include('partials/footer') -%>